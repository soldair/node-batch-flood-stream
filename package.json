{
  "name": "batch-flood-stream",
  "description": "emits batches of data events. grouped based on rate they are emitted and a max batch size.",
  "version": "0.0.0",
  "repository": {
    "url": "git://github.com/soldair/node-batch-flood-stream.git"
  },
  "main": "index.js",
  "scripts": {
    "test": "tape test/*.js"
  },
  "author": "Ryan Day",
  "dependencies": {
    "avg-stream": "~0.0.1",
    "through": "~2.3.4"
  },
  "devDependencies": {
    "tape": "~2.12.3"
  }
}
